{% extends 'base.html.twig' %}

{% block title %}Interet{% endblock %}

{% block body %}
    <h1>Interet</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ interet.id }}</td>
            </tr>
            <tr>
                <th>Experience</th>
                <td>{{ interet.experience }}</td>
            </tr>
            <tr>
                <th>Poste</th>
                <td>{{ interet.poste }}</td>
            </tr>
            <tr>
                <th>Salaire</th>
                <td>{{ interet.salaire }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ interet.status }}</td>
            </tr>
            <tr>
                <th>Activite</th>
                <td>{{ interet.activite }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('interet_index') }}">back to list</a>

    <a href="{{ path('interet_edit', {'id': interet.id}) }}">edit</a>

    {{ include('interet/_delete_form.html.twig') }}
{% endblock %}
